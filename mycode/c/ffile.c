#include <stdio.h>
int  main(int argc,const char argv[])
{
        FILE *fp=fopen("test","r+");
        if(fp==NULL)
        {
                printf("fopen fail\n");
                s
                return;
        }
        
        return 0;
}